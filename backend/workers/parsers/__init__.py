"""Parser workers (Milestone 3).

Turn a captured RawAnswer into structured rows: brand mention detection
(string match + LLM fallback), position extraction, sentiment scoring (LLM),
and source/URL extraction with domain classification.
"""
